# Instrodução ao Kubernetes (CLI básico)

kubectl get pods
kubectl describe pod name
kubectl logs nome
kubectl exec -it nome -- bash

# Voce pode criar scripts bash para automatizar deploys
# usando kubectl apply -f